getProjectCards:
  name: ${self:provider.stage}-getProjectCards
  role: ${self:provider.stage}-getProjectCardsRole
  handler: handlers/project/getProjectCards/handler.handler
  events:
    - http:
        path: /projects
        method: get
        cors: true
