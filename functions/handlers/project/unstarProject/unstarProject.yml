unstarProject:
  name: ${self:provider.stage}-unstarProject
  role: starProjectRole
  handler: handlers/project/unstarProject/handler.handler
  events:
  - http:
      path: /project/star
      method: delete
      cors: true
      authorizer:
        name: authorizer
        resultTtlInSeconds: 300
        identitySource: method.request.header.Authorization
        identityValidationExpression: .*
        type: token
