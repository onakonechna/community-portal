authorization:
    name: ${self:provider.stage}-authorization
    role: ${self:provider.stage}-authorizationRole
    handler: handlers/authorization/handler.handler
    events:
    - http:
        path: /authorize/
        method: post
        cors: true